# pi-open-terminal

在 Pi 中输入 `/d`，在 Pi 当前工作目录打开一个新的 Kitty 终端窗口。

## 安装

```bash
pi install /home/spike/funnythings/pi-extensions/open-terminal
```

如果 Pi 已经在运行，安装后执行：

```text
/reload
```

## 使用

```text
/d
```
